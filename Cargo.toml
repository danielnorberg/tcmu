[package]
name = "tcmu"
version = "0.1.0"
edition = "2024"
description = "Generic SCSI/TCMU command processor for implementing read-only user-space block devices"
license = "MIT"
repository = "https://github.com/danielnorberg/tcmu"

[dependencies]
anyhow = "1.0"
